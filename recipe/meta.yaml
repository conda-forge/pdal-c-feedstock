{% set name = "pdal-c" %}
{% set version = "2.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/PDAL/CAPI/archive/refs/tags/v2.1.1.tar.gz
  sha256: f10ad4592502b0c01e40f6faa0989ea6ef43f30d1c87adec3c76a2236f018dec

build:
  number: 2

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - cmake
    - make  # [not win]
    - ninja  # [win]
  host:
    - pdal
  run:
    - {{ pin_compatible('pdal', max_pin='x.x') }}

test:
  commands:
    - test_pdalc

about:
  home: https://github.com/pdal/capi
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.md
  summary: C API for the PDAL Point Cloud Data Abstraction Layer
  description: |
    PDAL is a C++ library for translating and manipulating point cloud data. This package provides a C API to the package.
  doc_url: https://pdal.io/CAPI/doxygen/html/index.html
  dev_url: https://github.com/pdal/capi

extra:
  recipe-maintainers:
    - Runette
    - hobu
